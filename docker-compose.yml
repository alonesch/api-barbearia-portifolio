version: "3.9"

services:
  db:
    image: postgres:16
    container_name: barbercloud-postgres-dev
    restart: unless-stopped

    env_file:
      - .env

    environment:
      POSTGRES_DB: ${POSTGRES_DB_DEV}
      POSTGRES_USER: ${POSTGRES_USER_DEV}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD_DEV}

    ports:
      - "5433:5432" # DEV exposto

    volumes:
      - postgres_dev_data:/var/lib/postgresql/data

    networks:
      - barbercloud_net

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER_DEV} -d ${POSTGRES_DB_DEV}"]
      interval: 10s
      timeout: 5s
      retries: 5


  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: barbercloud-api-dev
    restart: unless-stopped

    depends_on:
      db:
        condition: service_healthy

    env_file:
      - .env

    environment:
      ASPNETCORE_ENVIRONMENT: Development

      POSTGRES_CONNECTION_DEV: ${POSTGRES_CONNECTION_DEV}
      JWT__KEY: ${JWT__KEY}

      EMAIL_HOST_DEV: ${EMAIL_HOST_DEV}
      EMAIL_PORT_DEV: ${EMAIL_PORT_DEV}
      EMAIL_USER_DEV: ${EMAIL_USER_DEV}
      EMAIL_PASS_DEV: ${EMAIL_PASS_DEV}
      EMAIL_FROM_DEV: ${EMAIL_FROM_DEV}
      EMAIL_NAME_DEV: ${EMAIL_NAME_DEV}

      FRONTEND_URL_DEV: ${FRONTEND_URL_DEV}
      API_URL_DEV: ${API_URL_DEV}

      ASPNETCORE_URLS: http://0.0.0.0:8080

    ports:
      - "8080:8080"

    networks:
      - barbercloud_net


volumes:
  postgres_dev_data:


networks:
  barbercloud_net:
    name: barbercloud-dev_barbercloud_net
    driver: bridge
